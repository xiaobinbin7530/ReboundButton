<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="MySwitchButton">
        <attr name="leftText" format="string"/>
        <attr name="rightText" format="string"/>
        <attr name="textSize" format="dimension"/>
        <attr name="textPadding" format="dimension"/>
        <attr name="textCheckedColor" format="color"/>
        <attr name="textUnCheckedColor" format="color"/>
        <attr name="backRectColor" format="color"/>
    </declare-styleable>
    <declare-styleable name="CornerVew">
        <attr name="cv_text" format="string" />
        <attr name="cv_text_color" format="color" />
        <attr name="cv_text_size" format="dimension" />
        <attr name="cv_text_bold" format="boolean" />
        <attr name="cv_text_all_caps" format="boolean" />
        <attr name="cv_background_color" format="color" />
        <attr name="cv_min_size" format="dimension" />
        <attr name="cv_padding" format="dimension" />
        <attr name="cv_gravity" format="enum">
            <enum name="TOP_LEFT" value="51" />
            <enum name="TOP_RIGHT" value="53" />
            <enum name="BOTTOM_LEFT" value="83" />
            <enum name="BOTTOM_RIGHT" value="85" />
        </attr>
        <attr name="cv_fill_triangle" format="boolean" />
    </declare-styleable>
    <declare-styleable name="lbBaseGridView">
        <!-- Allow DPAD key to navigate out at the front of the View (where position = 0),
             default is false  -->
        <attr name="focusOutFront" format="boolean" />
        <!-- Allow DPAD key to navigate out at the end of the view, default is false -->
        <attr name="focusOutEnd" format="boolean" />
        <!-- Allow DPAD key to navigate out of first row, for HorizontalGridView, it's the
             top edge, for VerticalGridView it's the "start" edge.  Default value is true.  -->
        <attr name="focusOutSideStart" format="boolean" />
        <!-- Allow DPAD key to navigate out of last row, for HorizontalGridView, it's the
             bottom edge, for VerticalGridView it's the "end" edge.  Default value is true.  -->
        <attr name="focusOutSideEnd" format="boolean" />
        <!-- Defining margin between two items horizontally -->
        <attr name="horizontalMargin" format="dimension" />
        <!-- Defining margin between two items vertically -->
        <attr name="verticalMargin" format="dimension" />
        <!-- Defining gravity of child view -->
        <attr name="android:gravity" />
    </declare-styleable>
    <declare-styleable name="lbHorizontalGridView">
        <!-- Defining height of each row of HorizontalGridView -->
        <attr name="rowHeight" format="dimension" >
            <enum name="wrap_content" value="-2" />
        </attr>
        <!-- Defining number of rows -->
        <attr name="numberOfRows" format="integer" />
    </declare-styleable>

    <declare-styleable name="lbVerticalGridView">
        <!-- Defining width of each column of VerticalGridView -->
        <attr name="columnWidth" format="dimension" >
            <enum name="wrap_content" value="-2" />
        </attr>
        <!-- Defining number of columns -->
        <attr name="numberOfColumns" format="integer" />
    </declare-styleable>
    <declare-styleable name="BGAFlowLayout"><attr format="dimension" name="fl_horizontalChildGap"/><attr format="dimension" name="fl_verticalChildGap"/><attr format="boolean" name="fl_isDistributionWhiteSpacing"/></declare-styleable>
    <declare-styleable name="BGAProgressBar">
        <!-- 进度条样式，默认值为 system -->
        <attr name="bga_pb_mode" format="enum">
            <!-- 系统自带样式 -->
            <enum name="system" value="0"/>
            <!-- 水平进度条 -->
            <enum name="horizontal" value="1"/>
            <!-- 圆形进度条 -->
            <enum name="circle" value="2"/>
            <!-- 彗星进度条 -->
            <enum name="comet" value="3"/>
            <!-- 水波纹进度条 -->
            <enum name="wave" value="4"/>
        </attr>
        <!-- 文字颜色，默认值为 #70A800 -->
        <attr name="bga_pb_textColor" format="reference|color"/>
        <!-- 文字大小，默认值为 10sp -->
        <attr name="bga_pb_textSize" format="dimension"/>
        <!-- 文字和进度条之间的间距，默认值为 4dp -->
        <attr name="bga_pb_textMargin" format="dimension"/>
        <!-- 已完成进度的颜色，默认值为 #70A800 -->
        <attr name="bga_pb_reachedColor" format="reference|color"/>
        <!-- 已完成进度的高度，默认值为 2dp -->
        <attr name="bga_pb_reachedHeight" format="dimension"/>
        <!-- 未完成进度的颜色，默认值为 #CCCCCC -->
        <attr name="bga_pb_unReachedColor" format="reference|color"/>
        <!-- 未完成进度的高度，默认值为 1dp -->
        <attr name="bga_pb_unReachedHeight" format="dimension"/>
        <!-- 是否为圆角，默认值为 false -->
        <attr name="bga_pb_isCapRounded" format="boolean"/>
        <!-- 是否隐藏文字，默认值为 false -->
        <attr name="bga_pb_isHiddenText" format="boolean"/>
        <!-- 圆形进度条半径，默认值为 16dp -->
        <attr name="bga_pb_radius" format="dimension"/>
    </declare-styleable>
</resources>